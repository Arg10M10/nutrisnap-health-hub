import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.nutrisnaphealthhub',
  appName: 'nutrisnap-health-hub',
  webDir: 'dist'
};

export default config;
